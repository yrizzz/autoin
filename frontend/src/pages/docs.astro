---
import BaseLayout from '../layouts/BaseLayout.astro';
import ApiDocs from '../components/docs/ApiDocs';
---

<BaseLayout title="Dokumentasi API">
  <!-- client:only — halaman ber-auth ini di-render full di client (AdminLayout
       baca localStorage/sesi), jadi tak ada SSR yang bikin hydration mismatch. -->
  <ApiDocs client:only="react" />
</BaseLayout>
